import withModal  from '../../withModal';
import ModalComponent from './Modal';
import ModalButton from './ModalButton';

export { ModalComponent, ModalButton };

export default withModal(ModalComponent);
